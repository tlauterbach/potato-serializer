namespace PotatoSerializer {

	public sealed partial class Serializer : ISerializer {

		private void WriteString(string name, string value) {
			//m_currentObject.Add(name, value);
		}
		private void WriteString(string value) {
			//m_currentList.Add(value);
			//m_currentIndex++;
		}
		private void WriteNumber(string name, double value) {
			//m_currentObject.Add(name, )
		}

	}

}